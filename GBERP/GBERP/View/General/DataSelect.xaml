<UserControl x:Class="GBERP.View.General.DataSelect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GBERP.ViewModel.General"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024" Background="White">
    <UserControl.DataContext>
        <vm:DataSelect></vm:DataSelect>
    </UserControl.DataContext>
    <Grid>
        <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding DataSource}"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserResizeRows="True"
                  CanUserSortColumns="True"
                  AutoGeneratedColumns="DataGrid_AutoGeneratedColumns">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn IsReadOnly="False"></DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
